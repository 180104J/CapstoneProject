import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyp.csProject',
  appName: 'csProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
